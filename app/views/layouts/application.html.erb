<!DOCTYPE html>
<html>
  <head>
    <title>Tacostagram Final</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
  </head>

  <body>
  
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">  
      <div class = "container-fluid">
        <a class = "navbar-brand" href="/" >Tacostagram! </a>
           <ul class="navbar-nav mb-2 mb-lg-0">
              <% if @current_user%>
                  Logged in as: <%= @current_user["username"]%>
                  <a href="/" class="btn btn-warning me-2" > Home</a>
                  <a href="/logout" class="btn btn-warning me-2">Logout</a>                   
              <% else %>
                <a href="/login" class="btn btn-warning me-2">Login</a>
                <a href="/users/new" class="btn btn-warning me-2">Signup</a>
              <% end %>
            </ul>
      </div>
    </nav>
    
    <div class = "container m-3">
      <div class = "row">
        <div class = "col m-3" >
          <%= flash["notice"] %>
          <%= yield %>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
